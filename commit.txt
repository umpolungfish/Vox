I push the phase membrane to a 19,673-digit semiprime

I carry the encoded-N-only phase route through wider Proth semiprimes, record and repair the phase, gcd, radix-fold, and output walls, and add a compact reversible frame alphabet plus a power-of-two radix fast path. I retain the compiled trial binaries, verify the decoded factors and both closure supports, and record direct timings through the new 19,673-digit result.
