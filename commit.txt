I integrate the repaired banked EXTRACT word

I recognize ⊢∈≻⊤⋈⊙≺⊥⊞∋⊡⋈⊙⊣ as the one-frame EXTRACT followed by its
⊡⋈⊙ latch, while retaining the previous EXTRACT/FIX spelling. The exact word
factors 8051 through the resident carrier. The full library suite passes all
197 tests, the phase-register tests pass, and the release check emits no
warnings. I keep the dynamic-width phase trial silent through completion.
