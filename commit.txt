I remove the one-minute cutoff from the EML membrane runner and rerun the two widest base-2 semiprimes without a timeout. Both contained binaries close and return the exact encoded smaller factor; I retain their inputs, executables, outputs, and timings.
