I nest the multiplicative and comultiplicative factor closures

I carry the phase relation through the repaired banked EXTRACT word, then close
the phase-produced factor pair through product-outer/prefix-inner and
prefix-outer/product-inner compositions. I expose factors only where their
terminal fixed points coincide, and buffer the complete report until both
closures finish. I bake and run the 143 control and the 10-digit semiprime
5726623061; the latter returns 43691 × 131071 in 164.211 μs in-process. The
full library suite passes 199 tests, the binary suite passes five tests, and
the release check is warning-free. I include both contained binaries and the
updated timing record.
