I unbraid the baked support inside evaluation frames with the running-product register, return both factor words through the same frames, and close their product on N. I include the compiled membrane and verify the route with warnings-denied tests and an asymmetric semiprime execution.
