I bake my phase and semiprime inputs into per-case IMASM binaries

I compile each campaign case with canonical IMASM numerals and retain its
executable and hash. I remove runtime numeric inputs from the probe and test
that runtime overrides leave the baked phase observations unchanged.
I implement observation banking across descent and fusion, test exact payload
preservation, and document the remaining blueprint execution and acquisition work.
