I join phase closures into dual factor registers

I seed the two factor registers from the complementary phase half-step closures, advance both Euclidean arms to their shared product boundary, and run the multiplicative-first and radix-prefix-first nestings in the baked membrane. I verify their common fixed point on a wide phase case and retain the compiled binary.
