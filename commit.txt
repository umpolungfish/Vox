I record the compiled-only escalation boundary and the focused Vox audit path.

I document that wide scaling audits and lifts binaries without rerunning the
slower serialized execution path. I retain the focused VM failure evidence.
