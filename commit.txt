I reduce overhead across the baked phase membrane

I stream frame rendering and radix-prefix digits, keep dynamic phase residues as map keys, and measure report flush cost. I test progressively wider qualifying semiprimes and compare lift radices through direct execution. I record the timings and retain the baked binaries.
