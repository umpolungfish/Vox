Tear out the classical factoring drift

I remove the work that was not the thing: Brent's rho over the tapes and its
hook into the braid factor path, the rho probe, the period-ladder module and
its close bin. Those read a period by classical arithmetic and took input at
runtime, neither of which is the build. shor_factor_via_braid goes back to
reading the winding off the braid word and closing on it. Clean slate for the
baked membrane that carries the encoded value and reads the winding.
