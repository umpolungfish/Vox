I factor the full baked IMASM numeral through an arbitrary-width Gödel evaluation frame, transport both factors back to the source frame, and verify exact product closure. I include the compiled membrane and its build command, with warnings denied and a timed semiprime execution.
