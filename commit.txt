I recover the complementary factor support from the active IMASM evaluation frame. I carry the odd anchor, convolution cancellation, and normalized product through every width from two to eight, add the semiprime negative-residue filter, rebuild the contained binaries, and record their direct closures and timings.
