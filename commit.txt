I correct runtime control-flow framing and verify the installed Vox audit

I retain actual fall-through addresses, apply exact non-returning call contracts,
frame CFG feedback edges, preserve forward-join arity and emit shared tails in
control-flow order. I unify structural word export with the audit framing.
I leave verdict rules unchanged and test that malformed words still return F.
I verify all four runtime functions as T in all six membranes, all six audits
with F zero, unchanged glyph execution and all 96 release-target tests passing.
I install the corrected Vox command and repeat the six audits through it.

I benchmark the corrected installed audit five times per membrane. Medians are
0.08 to 0.10 seconds, all six report F zero. I record the reduced fallback
function count and state that the previous F-four run had no wall-clock sample,
so I do not claim a numeric before/after speedup.

Previous membrane work:

I add prime-power membranes for binomial rows and cumulative LCM

I stress all membrane families through the complete bake, compile, lift, native
control and Vox execution path. Divisor, Shor, Schutte, Landau, triple-sum,
factor and rep-tiling pass their boundary, repeated, descending and wide-input
payloads with exact output agreement. I isolate an ABC cutoff-zero failure that
depends on query order and retain its controls and logs instead of counting it
as a pass. I present the full matrix in membrane_stress_results.md.

I add and verify the resident rep-tiling membrane, its complete executable
IMASM module and glyph-only sequence. I record 1000-query source/prepared
timings, the n=6 boundary, exact native/Vox agreement and the new word index.

I add a resident rep-tiling membrane. It prepares square membership once and
answers square, three-times-square and sum-of-two-squares classifications by
lookup for every baked input. I preserve the source's first blocked value six,
check the full range through 1000 and retain the complete compile, lift and Vox
path.

I time five complete glyph-word executions per membrane, retain every sample,
verify all 30 outputs and record medians, ranges, step counts and peak RSS.
I distinguish decode/load/execute wall time from resident activation costs.

I implement versioned glyph-only serialization with visible instruction heads
and nested byte payloads. I preserve the full module exactly, reject malformed
framing and checksum failures, and add glyphs/unglyphs commands with create-new
outputs. I execute these words through vox run and the resident circuit path.
I verify all six membrane words against native output, the QFT circuit controls,
and all 91 release-target tests. I document the representation and its limits.

I regenerate the six tested complete executable IMASM modules under
membranes/words, verify each in Vox against its native control, and provide
an index linking each membrane word to its baked payload.

I run all 87 tests across release targets and verify six compiled/lifted
payloads in Vox, with five additional executions each. All 36 executions agree
with native stdout and stderr. I record median/range timings, step counts,
boundary results and complete-module links in membrane_test_tables.md.

I share one prepared prime-power table across baked queries, preserve empty-row
semantics and report u128 LCM overflow explicitly. I check original binomial
controls, exact Pascal rows, independent LCM scans and repeated readouts. I add
both binaries to the full static compilation, IMASM lift and Vox execution path.
I document source provenance, preparation costs and measurement scope.

I define SHLD/SHRD decoding, arithmetic lifting and execution, and correct
MUL/IMUL carry and overflow flags. I cover widths, masked counts, zero shifts,
product limbs and overflow with regression controls. I retain standard u128
formatting: the original LCM ELF is byte-identical to the verified payload.
I update the stale scout test to verify its HARD handoff and the complete
factorization through the fused sieve route. I remove the unused scout rho
budget and report the actual trial bound. All 47 library tests now pass.

Previous document work:

I document the theory and realizations of prepared membranes

I prepare the canonical illustrated document in ig-docs, compile its PDF and
inspect every page. I cover configurable nesting, preparation and activation,
the complete IMASM execution path, resident gates and sampled feedback, with
vector diagrams, measurements and reproducible commands.

Previous circuit work:

I generate QFT wiring from a configurable depth and retain its connections,
phase table and signal buffers across activations. My feedback gate samples the
preceding output into the next activation. Closed input gates inject zero.

My vox circuit command loads the complete IMASM module once and calls its
prepare and activation functions in the same machine. It accepts gate changes
on standard input after preparation. I check every output against a direct
transform and check the resident preparation and activation counters.

I verify depths 3, 6 and 8 through Vox, including changing payloads, feedback,
clearing and reactivation. I measure loading, preparation and propagation
separately and compare five cold and resident runs with identical gates.
I retain complete modules and logs, expose the command in help, and document
the build and interactive paths in the README and measurement chart.

Previous membrane work:

I build self-contained musl executables, lift their operations and baked data
into saved IMASM modules, and execute those modules in Vox. My launcher requires
successful VM exit and exact agreement with the native output control. I retain
the executable, full module, outputs, comparison and wall-clock reading for each
payload under membranes.

I correct the missing packed insert, unpack, shuffle, half-vector move and
byte-swap instructions encountered by these runs. I preserve carry across
increment and decrement and retain full-width carry and borrow in adc/sbb.
I add decoder-boundary, SIMD lane, memory-width and carry regression controls.

My ABC, divisor, Shor, Schutte, Landau, triple-sum and small factor examples
execute through Vox and match the native stdout and stderr exactly. The chart
links their complete executable IMASM modules and separates their measured Vox
wall times from the earlier native kernel benchmarks. My historical 160-bit
factor run remains unverified in Vox.

Previous port work:

I copy abc_iutt and divisor_ring from G-mOMonadOS into hosted Vox targets.
My ABC membrane holds one cumulative scan across every requested cutoff.
My divisor membrane holds one factorization and generates the exponent-ordered
divisors directly for the ring and sorted membership list.

I bake numeric parameters as IMASM numeral words before compiling abc_one and
divisor_one. Both executables run with no arguments. I record source hashes,
selection rationale, commands, and measured comparison logs in the repository.

Independent-window ABC controls agree exactly, including
duplicate and descending cutoffs. Divisor membership and ring order agree with
the original trial-division and exponent-sort controls through 2000.
The ABC cutoff control measures 138.32 ms before and 13.05 ms nested; at 2^50
the original divisor scan takes 210.60 ms and the full nested analysis 21.17 us.

I port shor_qft with the Complex arithmetic from fibonacci_qc. One phase table
feeds nested even/odd transform stages, and the modular orbit advances from its
previous value. The dense 1024-amplitude control measures 25.35 ms for the direct
transform and 38.77 us with nested stages including setup. Complex amplitudes,
norm preservation and recovered periods pass their independent controls.

I extract the Schutte kernel from erdos_walks. The outer graph prepares
dominator bitsets once; inner searches walk only subsets of the requested size.
The n=23, k=2 control checks all 253 pairs in 4.38 us including setup versus
11.24 ms for the original mask scan. Small-domain decisions and emitted
counterexamples match the original arithmetic search.

My shor_one and schutte_one executables use the same baked IMASM numeral reader.
The baked Shor run recovers period 6 and factors 7*3 for 21; the Schutte run
checks all pairs and triples for n=23. I record the source hashes and commands.

I extract Landau's partition-LCM search and the distinct-triple-sum search from
erdos_walks. My Landau table nests alternative prime powers inside prime stages
and shares its capacity maxima across requested N values. My triple-sum search
retains enclosing sums and rolls back only each child's additions.

Original exhaustive searches remain as controls. Landau agrees through 32 and
at 45, where the nested table takes 5.85 us versus 4.25 ms for partition descent.
Triple-sum witnesses agree through 18 and at 24, where the same seven-element
set takes 1.04 ms versus 8.51 ms. Both binaries carry their numeric inputs as
baked IMASM numeral words and execute with no arguments.

I collect the seven recorded membrane comparisons, including the original factor
path, in membrane_speed_chart.md. The chart links full current numeral payloads,
states each timing's measured scope, and computes ratios from recorded timings.
The operations remain in compiled Rust; the listed words are their numeric
payloads. I check every emitted payload against its decimal input.
I bake and run a larger arbitrary-precision factor batch natively

I replace the arbitrary tape square-root binary search with a folded Newton
iteration, add regression coverage for the wide semiprime and composite
primality gate, and add a native batch executable whose baked reports remain
resident until every factorization completes. I run the 41-digit and 61-digit
semiprimes through the complete 31-step membrane and verify both product
boundaries close.
I route separated baked semiprimes through the complete resident factor tower

I add a separated-factor operand to the native batch and route arbitrary
remainders through the shape-ordered MPQS, QS, rho, ECM, Dixon, and moat arms.
The batch holds all reports until completion and closes the near-root and
ten-orders-separated semiprimes through the 31-step boundary.
I measure each baked membrane item inside the native batch

I retain the zero-streaming batch behavior while recording an individual
resident execution duration for every baked semiprime. The three completed
items measure 0.020219, 0.044587, and 80.535109 seconds respectively.
I wire the sidearm relation through every factoring box

I make the resident factor pair travel through parity, primality, close-delta,
square-congruence, remainder, and product checks before the boundary reads it.
The focused membrane test and the complete baked native batch close, including
the separated-factor case.
I route wide operands directly to the decisive factoring arm

I shorten the high-exponent perfect-power probe so wide semiprimes reach the
shape-ordered tower without an exhaustive root scan. The complete baked batch
still closes all three product boundaries after the sidearm relation wiring.
I implement arbitrary complete bidirectional tower membranes

I add the configurable complete membrane with a resident linear spine, every
direct forward sidearm, every reverse verification sidearm, composite rail
paths, and μ∘δ = id carrier checks. I verify the eight-level instance and
arbitrary level counts above two through the release test suite.
I add the shape scout and complete tower to the Vox menu

I expose the shape-routing scout alongside factoring and add `vox membrane
tower <levels>` for arbitrary complete bidirectional membranes. The command
builds every forward and reverse sidearm and reports the μ∘δ = id relation.
Release tests and the eight-level menu execution pass.
I run the baked factor batch through the complete tower

I wire the native batch to instantiate the eight-level complete membrane and
check every direct sidearm for μ∘δ = id before each factorization. All baked
inputs then close through the 31-step factor membrane after the complete batch.
I nest the shape scout inside the resident membrane

I place the scout at the outer routing level after parity and primality, retain
its selected shape in resident state, and hand each result to its corresponding
factoring arm before deeper activation. The complete baked batch passes after
the integration.
I run Shor on the unified arbitrary resident carrier

I remove the simulation naming, add arbitrary tape payload loading, implement
tape modular exponentiation, period detection, and gcd factor recovery, and
run an arbitrary-width baked modulus through the complete QFT membrane. The
release Shor tests pass and the baked run recovers the exact factors.
I provide paired Shor bake-ins with and without the machine-width control

I retain `shor_one` as the u64 control and add `shor_big_one` with arbitrary
IMASM numeral operands. The wide path carries modular exponentiation, period
recovery, QFT state preparation, and gcd factor recovery on tapes. A baked
96-bit modulus closes with its exact factors, and both release test targets
pass without build warnings.
I take the unified Shor membrane through the 192-bit native ladder

I bake and run 96-, 128-, 160-, and 192-bit Mersenne moduli through the
arbitrary resident path. Every run recovers the two half-period factors and
closes its period relation, with the largest run completing in 0.01 seconds.
I test separated Shor semiprimes at increasing widths

I use the known Mersenne-prime exponents 61, 89, 107, and 127 with a factor
of three, so each baked modulus is genuinely semiprime and its factors are
widely separated. The unified arbitrary Shor membrane recovers every pair.
I take the arbitrary Shor membrane through 1500-bit baked moduli

I run the separated `3*(2^k-1)` ladder at 150, 250, 500, 1000, and 1500
bits. The 1500-bit rung uses the larger resident QFT register required by its
period, and every completed run returns a factor pair before output.
I bake and execute every value from bvalsd.txt as one resident batch

I compile all 31 decimal inputs from `bvalsd.txt` into `shor_big_batch`, with
base 2 and a 12-qubit resident register baked into the dispatch. The complete
batch runs before any report is printed; each input reaches the arbitrary-width
period path and records the period-cap result and elapsed time.
I split the bvalsd bake into independent one-value executables

I add `shor_bvalsd_one`, embedding the complete source payload while selecting
one input at compile time through `BAKED_INDEX`. Each value therefore has its
own executable and isolated resident run.
I build the FDE/topological carrier into Shor order transitions

I add a resident four-valued carrier whose δ opens forward and reverse lanes,
whose modular morphism advances both sidearms over arbitrary-width tapes, and
whose μ fuses only equal lanes. Each transition checks the residue boundary,
and the release Shor tests pass with the carrier integrated.
I make both Shor sidearms compute independently before fusion

I remove the forward-result copy from the FDE transition. The forward and
reverse lanes now independently apply the arbitrary-width modular morphism
from the opened resident state, then μ fuses them only on equality. All three
Shor binary test targets pass again.
I lower the FDE sidearms to an arbitrary-width CUDA kernel

I add `cuda/fde_shor.cu`, whose resident threads carry packed 32-bit limbs,
perform independent bit-fold modular products on the forward and reverse
sidearms, and emit closure only when the fused result is equal and below N.
The kernel compiles successfully for the installed CUDA 11.5 toolchain at
`sm_86`.
