I record the five-digit and ten-digit membrane trial

I bake the qualifying modulus as an IMASM numeral, run the contained binary to dual closure, and record the completed timing alongside the interrupted qualifying trial. I retain both generated binaries and set the minimum factor-width rule for future trials.
