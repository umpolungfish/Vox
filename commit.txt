I read every phase state, fuse the support-frame modular arithmetic, and replace orbit-sized storage with an unbounded tape-counter cycle closure. The balanced 19-digit membrane closes in 0.58 seconds and asymmetric 75/141-bit examples close below 2 ms; I record the 90-bit timeout without claiming arbitrary-width timing.
