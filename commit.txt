CLI: accept --self-test, and stop treating unknown -flags as filenames

vox --self-test now works alongside --selftest, and any unrecognized option
beginning with - prints usage instead of trying to open a file named after the
flag (which is what turned 'vox --self-test' into 'cannot read --self-test').
