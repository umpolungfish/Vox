I remove arbitrary braid orbit budgets and probe extraction paths

I run phase, braid, and symbolic extraction directly on the 175-bit controls
and retain their stage diagnostics. I remove the fixed braid orbit cutoff,
replace bounded base scanning with tape residue traversal, and make external
campaign timeouts opt-in. I test closure beyond the former cutoff, winding
readout, factor reconstruction, and invalid-input rejection.
